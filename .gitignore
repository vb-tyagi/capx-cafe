node_modules/
dist/
.turbo/
coverage/
*.log
.env
.env.*
!.env.example
.DS_Store
.next/
.gcp/

# .mcp.json is COMMITTED (sanitized: no email/client-id/paths) so directory scanners
# (cursor.directory et al.) can discover the MCP server. Personal config with real values
# lives in .mcp.local.json, which stays ignored.
.mcp.local.json

# esbuild bundle output (the publishable capx-cafe package; regenerated by `pnpm --filter @capx-cafe/mcp build`)
apps/capx-mcp/dist/

# session handoffs (internal, never publish)
.ai/
